# SolSniper Pro - 完整运行指南

---

**作者**: Aitachi
**邮箱**: 44158892@qq.com
**微信**: 18116011230

---

## 🎯 当前状态

### ✅ 服务已启动
```
前端开发服务器: http://localhost:5176 (运行中，CORS已启用)
Mock API服务器: http://localhost:3000 (运行中)
```

### ✅ 文件已修复
```
- tsconfig.node.json (已创建)
- mock-api-server.js (已创建)
- test-page.html (已创建并更新端口)
- vite.config.ts (已启用CORS)
```

---

## 📋 三种访问方式

### 方式 1: 测试页面（推荐首选）

这是一个独立的测试页面，可以快速验证系统状态。

**访问地址**:
```
file:///C:/Users/ASUS/Desktop/B-partjob/solsinapor/SolSniperPro-main/test-page.html
```

**操作步骤**:
1. 在Windows资源管理器中找到 `test-page.html`
2. 双击打开（会在默认浏览器中打开）
3. 点击 "Run Tests" 按钮
4. 查看所有测试结果
5. 点击 "Open Main App" 进入主应用

**功能**:
- ✅ 自动检测前端服务器状态
- ✅ 自动检测API服务器状态
- ✅ 测试所有API端点
- ✅ 显示详细测试结果
- ✅ 一键跳转到主应用

---

### 方式 2: 主应用

访问完整的React前端应用。

**访问地址**:
```
http://localhost:5176
```

**预期显示**:
- 如果未登录 → 自动跳转到登录页 (/login)
- 如果已登录 → 显示仪表板 (/)

**登录页面特征**:
- 深色背景（#020617）
- 蓝色渐变Logo
- 登录表单
- 默认凭据提示

---

### 方式 3: 直接登录页

直接访问登录页面。

**访问地址**:
```
http://localhost:5176/login
```

**登录凭据**:
```
用户名: admin
密码: admin123
```

---

## 🔍 故障排查

### 问题: 浏览器显示 "无法访问此网站"

**原因**: 服务器未运行

**解决方案**:
```bash
# 检查前端服务器
netstat -ano | findstr :5176

# 如果没有输出，需要重启前端
cd frontend
npm run dev
```

---

### 问题: 白屏，无内容显示

**可能原因 1**: 浏览器缓存

**解决方案**:
```
按 Ctrl + Shift + R 强制刷新
```

**可能原因 2**: JavaScript错误

**解决方案**:
```
1. 按 F12 打开开发者工具
2. 查看 Console 标签页
3. 截图错误信息
```

**可能原因 3**: CSS未加载

**解决方案**:
```bash
# 清除Vite缓存
cd frontend
rm -rf node_modules/.vite
npm run dev
```

---

### 问题: 显示 "Loading..." 或一直转圈

**原因**: API服务器未响应

**解决方案**:
```bash
# 检查API服务器
curl http://localhost:3000/api/v1/health

# 如果失败，重启Mock API
node mock-api-server.js
```

---

### 问题: 登录后报错或无法跳转

**原因**: API返回错误

**检查步骤**:
```bash
# 测试登录API
curl -X POST http://localhost:3000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d "{\"username\":\"admin\",\"password\":\"admin123\"}"

# 应该返回:
# {"success":true,"data":{"token":"mock_jwt_token_12345",...}}
```

---

## 🛠️ 完整重启流程

如果遇到任何问题，按以下步骤完全重启：

### 步骤 1: 停止所有服务
```
在每个终端窗口按 Ctrl + C
```

### 步骤 2: 杀死占用端口的进程（如果需要）
```bash
# 查找占用5175端口的进程
netstat -ano | findstr :5175

# 杀死进程（替换<PID>为实际进程ID）
taskkill /PID <PID> /F

# 查找占用3000端口的进程
netstat -ano | findstr :3000

# 杀死进程
taskkill /PID <PID> /F
```

### 步骤 3: 启动Mock API服务器
```bash
# 打开第一个终端
cd C:\Users\ASUS\Desktop\B-partjob\solsinapor\SolSniperPro-main
node mock-api-server.js

# 等待看到：
# "Mock API Server running on http://localhost:3000"
```

### 步骤 4: 启动前端开发服务器
```bash
# 打开第二个终端
cd C:\Users\ASUS\Desktop\B-partjob\solsinapor\SolSniperPro-main\frontend
npm run dev

# 等待看到：
# "Local: http://localhost:5175/"
```

### 步骤 5: 访问应用
```
1. 打开test-page.html验证
2. 或直接访问 http://localhost:5176
```

---

## 📊 验证清单

使用以下清单确认系统正常运行：

### 服务器检查
- [ ] Mock API运行在3000端口
- [ ] 前端运行在5176端口
- [ ] 无端口冲突错误

### 功能检查
- [ ] test-page.html 所有测试通过
- [ ] http://localhost:5176 能正常访问
- [ ] 登录页面正常显示
- [ ] 使用admin/admin123能成功登录
- [ ] 登录后跳转到仪表板
- [ ] 仪表板显示数据

### UI检查
- [ ] 页面有深色背景
- [ ] Logo显示为蓝色渐变
- [ ] 文字清晰可读
- [ ] 按钮有hover效果
- [ ] 响应式布局正常

---

## 🎨 预期UI效果

### 颜色主题
```
主色调: 蓝色 (#0ea5e9)
背景色: 深蓝黑 (#020617)
卡片背景: 深灰 (#1e293b)
文字: 浅灰/白色 (#f1f5f9)
成功: 绿色 (#10b981)
危险: 红色 (#ef4444)
警告: 橙色 (#f59e0b)
```

### 字体
```
主字体: -apple-system, Segoe UI, Roboto
代码字体: Menlo, Monaco, Consolas
```

---

## 📸 截图对比

### 正常的登录页应该显示：
```
✅ 蓝色渐变的⚡图标
✅ "SolSniper Pro" 渐变文字标题
✅ "Professional Solana Token Trading Platform" 副标题
✅ 深色圆角登录卡片
✅ Username输入框
✅ Password输入框
✅ Sign In按钮
✅ "Default credentials: admin / admin123" 提示
✅ 版本号 "v2.0.0 © 2025 SolSniper Pro"
```

### 正常的仪表板应该显示：
```
✅ 顶部导航栏（Header）
✅ 左侧边栏（Sidebar）
✅ 核心指标卡片（4个）
✅ 活跃持仓列表
✅ 最近交易列表
```

---

## 🔧 开发者工具使用

### Console 标签
查看JavaScript错误和日志：
```javascript
// 应该看到：
[vite] connecting...
[vite] connected.

// 不应该看到：
❌ Failed to fetch
❌ TypeError: ...
❌ SyntaxError: ...
```

### Network 标签
查看网络请求：
```
✅ main.tsx - 200 OK
✅ App.tsx - 200 OK
✅ index.css - 200 OK
✅ /api/v1/... - 200 OK
```

### Application 标签
查看LocalStorage：
```
auth-storage: {token: "...", user: {...}}
```

---

## 📞 获取帮助

如果以上方法都不能解决问题，请准备以下信息：

### 必需信息
1. **浏览器信息**
   - 浏览器类型（Chrome/Edge/Firefox）
   - 浏览器版本号

2. **错误信息**
   - Console标签的完整错误
   - Network标签的失败请求
   - 页面显示的错误提示

3. **服务器输出**
   - 前端终端的完整输出
   - Mock API终端的完整输出

4. **测试结果**
   - test-page.html的测试结果截图

5. **截图**
   - 浏览器显示的页面
   - 开发者工具的错误

### 联系方式
- **邮箱**: 44158892@qq.com
- **微信**: 18116011230

---

## 🚀 快速命令参考

### 启动命令
```bash
# Mock API
node mock-api-server.js

# 前端
cd frontend && npm run dev
```

### 测试命令
```bash
# 测试API健康
curl http://localhost:3000/api/v1/health

# 测试登录
curl -X POST http://localhost:3000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d "{\"username\":\"admin\",\"password\":\"admin123\"}"

# 测试前端
curl http://localhost:5176/
```

### 端口检查
```bash
# Windows
netstat -ano | findstr :5176
netstat -ano | findstr :3000

# Git Bash
netstat -ano | grep :5176
netstat -ano | grep :3000
```

### 进程管理
```bash
# 结束进程
taskkill /PID <进程ID> /F

# 查看所有Node进程
tasklist | findstr node
```

---

## ✅ 成功标志

系统正常运行时，你会看到：

1. ✅ **test-page.html** - 所有测试显示绿色PASS
2. ✅ **前端终端** - "Local: http://localhost:5176/"
3. ✅ **API终端** - "Mock API Server running on http://localhost:3000"
4. ✅ **浏览器** - 登录页面正常显示
5. ✅ **登录** - admin/admin123能成功登录
6. ✅ **仪表板** - 数据正常显示

---

## 📝 附录

### A. 目录结构
```
SolSniperPro-main/
├── frontend/              # React前端
│   ├── src/
│   ├── package.json
│   └── vite.config.ts
├── mock-api-server.js     # Mock API
├── test-page.html         # 测试页面
└── 前端显示问题排查指南.md
```

### B. 依赖版本
```
Node.js: v24.11.1
npm: 11.6.2
Vite: 5.4.21
React: 18.2.0
TypeScript: 5.2.2
```

### C. 端口使用
```
5176: Vite开发服务器 (启用CORS)
3000: Mock API服务器
```

---

**文档创建**: 2025-12-21
**最后更新**: 2025-12-21
**版本**: v1.0

---

<p align="center">
  Made with ❤️ by Aitachi
</p>
