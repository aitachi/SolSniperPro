# SolSniper Pro - 快速启动指南（中文版）

---

**作者**: Aitachi  
**邮箱**: 44158892@qq.com  
**微信**: 18116011230

---

**完整系统启动指南**
## 系统要求

### 必需软件
- Rust 1.75+ (`rustc --version` 检查版本)
- Node.js 18+ (`node --version` 检查版本)
- Docker & Docker Compose (`docker --version` 检查版本)
- PostgreSQL 16 (通过 Docker 或本地安装)
- Redis 7 (通过 Docker 或本地安装)
- Kafka (通过 Docker 或本地安装)

### 硬件要求
- CPU: 4核心以上
- 内存: 8GB 以上
- 硬盘: 20GB 可用空间
- 网络: 稳定的互联网连接
## 🚀 快速启动（自动化）

### 方式一：使用启动脚本（推荐）

```bash
# 进入项目根目录
cd SolSniperPro-main

# 赋予脚本执行权限
chmod +x scripts/start.sh

# 启动所有服务
./scripts/start.sh

# 或强制清理后启动
./scripts/start.sh --force

# 或以生产模式启动
./scripts/start.sh --production
```

启动脚本将自动执行以下操作：
1. ✅ 检查所有依赖项
2. ✅ 创建必要的目录
3. ✅ 验证配置文件
4. ✅ 启动 Docker 容器（Kafka、PostgreSQL、Redis）
5. ✅ 初始化数据库
6. ✅ 编译后端（Rust）
7. ✅ 启动 API 服务器
8. ✅ 构建并启动前端

### 检查启动状态

启动成功后，您将看到：

```
=====================================
   SolSniper Pro - 系统状态
=====================================

✅ Docker 容器: 运行中
✅ PostgreSQL: 就绪 (端口 5432)
✅ Redis: 就绪 (端口 6379)
✅ Kafka: 就绪 (端口 9092)
✅ 后端 API: 运行中 (端口 3000)
✅ 前端: 运行中 (端口 5173)

=====================================
   访问地址
=====================================

前端界面:  http://localhost:5173
API 接口:  http://localhost:3000
API 文档:  http://localhost:3000/api/v1/docs

默认登录信息:
  用户名: admin
  密码: admin123

=====================================
```
## 🔧 手动启动（分步骤）

### 第一步：启动基础设施

```bash
# 启动 Docker 容器
docker-compose up -d

# 验证容器运行状态
docker ps

# 预期输出：
# - solsniper-postgres (端口 5432)
# - solsniper-redis (端口 6379)
# - solsniper-kafka (端口 9092)
# - solsniper-zookeeper (端口 2181)
```

### 第二步：初始化数据库

```bash
# 连接到 PostgreSQL
psql -h localhost -U solsniper -d solsniper_db

# 运行初始化脚本
\i scripts/init_db.sql

# 验证表是否创建
\dt

# 退出
\q
```

### 第三步：配置环境变量

在项目根目录创建 `.env` 文件：

```env
# 数据库配置
DATABASE_URL=postgresql://solsniper:solsniper123@localhost:5432/solsniper_db

# Redis 配置
REDIS_URL=redis://localhost:6379

# Kafka 配置
KAFKA_BROKERS=localhost:9092

# Solana RPC 节点
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
SOLANA_WS_URL=wss://api.mainnet-beta.solana.com

# 服务器配置
API_PORT=3000
LOG_LEVEL=info

# 安全配置
JWT_SECRET=your-secret-key-change-in-production
```

### 第四步：编译并运行后端

```bash
# 以发布模式编译
cargo build --release

# 运行 API 服务器
cargo run --bin api-server --release

# 或直接运行编译后的文件
./target/release/api-server
```

预期输出：
```
[INFO] 正在启动 SolSniper Pro API 服务器
[INFO] 数据库已连接: PostgreSQL
[INFO] Redis 已连接: localhost:6379
[INFO] Kafka 已连接: localhost:9092
[INFO] API 服务器监听于 0.0.0.0:3000
[INFO] WebSocket 服务器就绪
```

### 第五步：编译并运行前端

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install

# 开发模式运行
npm run dev

# 或生产环境构建
npm run build
npm run preview
```

预期输出：
```
VITE v5.0.8  ready in 324 ms

➜  本地:   http://localhost:5173/
➜  网络:   http://192.168.1.100:5173/
```
## 📱 访问应用程序

### 1. 打开浏览器

访问：**http://localhost:5173**

### 2. 登录系统

```
用户名: admin
密码: admin123
```

### 3. 开始使用

- **仪表板** - 交易指标概览
- **代币监控** - 监控新代币
- **策略管理** - 管理交易策略
- **交易管理** - 查看交易历史
- **持仓管理** - 监控活跃持仓
- **风险控制** - 配置风险设置
- **数据分析** - 性能分析
- **系统设置** - 用户偏好设置
## 🛠️ 常见问题解决

### 后端无法启动

**问题**：数据库连接错误

```bash
# 检查 PostgreSQL 是否运行
docker ps | grep postgres

# 测试连接
psql -h localhost -U solsniper -d solsniper_db -c "SELECT 1;"

# 重启容器
docker restart solsniper-postgres
```

**问题**：Kafka 连接错误

```bash
# 检查 Kafka 是否运行
docker ps | grep kafka

# 查看 Kafka 日志
docker logs solsniper-kafka

# 重启
docker restart solsniper-kafka solsniper-zookeeper
```

### 前端无法连接

**问题**：API 无法访问

```bash
# 检查 API 是否运行
curl http://localhost:3000/api/v1/health

# 检查代理配置
cat frontend/vite.config.ts | grep proxy
```

**问题**：WebSocket 断开连接

```bash
# 检查 WebSocket 端点
curl -i -N \
  -H "Connection: Upgrade" \
  -H "Upgrade: websocket" \
  http://localhost:3000/ws

# 应返回: 101 Switching Protocols
```

### 数据库问题

```bash
# 重置数据库
docker stop solsniper-postgres
docker rm solsniper-postgres
docker volume rm solsniper_postgres_data

# 重新启动
docker-compose up -d postgres

# 重新初始化
psql -h localhost -U solsniper -d solsniper_db -f scripts/init_db.sql
```

### 端口被占用

```bash
# Windows 系统 - 查找占用端口的进程
netstat -ano | findstr :3000
netstat -ano | findstr :5173

# 杀死进程（使用上面命令找到的 PID）
taskkill /PID <进程ID> /F

# Linux/Mac 系统
lsof -ti:3000 | xargs kill -9
lsof -ti:5173 | xargs kill -9
```
## 🛑 停止系统

### 停止所有服务

```bash
# 使用停止脚本
./scripts/stop.sh
```

或手动停止：

```bash
# 停止前端（在终端按 Ctrl+C）

# 停止后端（在终端按 Ctrl+C）

# 停止 Docker 容器
docker-compose down

# 或停止并删除数据卷
docker-compose down -v
```
## 📊 系统监控

### 查看日志

**后端日志**：
```bash
# 如果使用 systemd 运行
journalctl -u solsniper-api -f

# 如果在终端运行
# 日志会直接显示在控制台
```

**Docker 日志**：
```bash
# PostgreSQL
docker logs -f solsniper-postgres

# Redis
docker logs -f solsniper-redis

# Kafka
docker logs -f solsniper-kafka
```

### 监控资源使用

```bash
# 检查 Docker 资源使用
docker stats

# 检查 API 健康状态
curl http://localhost:3000/api/v1/health

# 检查系统指标
curl http://localhost:3000/api/v1/metrics/summary
```
## 🔐 生产环境部署前检查清单

部署到生产环境前，请确保：

- [ ] 修改 `.env` 中的 `JWT_SECRET`
- [ ] 修改数据库密码
- [ ] 修改 Redis 密码（如果暴露）
- [ ] 启用 HTTPS/TLS
- [ ] 配置防火墙规则
- [ ] 设置监控和告警
- [ ] 启用速率限制
- [ ] 检查 CORS 配置
- [ ] 实施备份策略
- [ ] 更新默认管理员密码
## 📚 相关文档

- **系统架构文档**: `docs/01_SYSTEM_ARCHITECTURE.md`
- **策略指南**: `docs/02_STRATEGY_GUIDE.md`
- **API 参考**: `docs/03_API_REFERENCE.md`
- **部署指南**: `docs/04_DEPLOYMENT_GUIDE.md`
- **前端架构**: `docs/05_FRONTEND_ARCHITECTURE.md`
- **实现总结**: `docs/06_FRONTEND_IMPLEMENTATION_SUMMARY.md`
## ✅ 成功启动的标志

当您的系统正常工作时，应该看到：

1. ✅ 所有 Docker 容器状态为 "Up"
2. ✅ 后端日志显示 "API server listening"
3. ✅ 前端加载无控制台错误
4. ✅ 登录成功并跳转到仪表板
5. ✅ WebSocket 显示 "已连接"（检查开发者工具）
6. ✅ 仪表板显示指标（即使为零）
7. ✅ 页面间导航正常工作
8. ✅ 实时更新正常显示（检查控制台）
## 🆘 获取帮助

### 检查文档

1. 阅读 `docs/` 目录中的相关文档
2. 查看各组件目录中的 README 文件
3. 查看代码注释了解具体实现

### 常见问题对照表

| 问题 | 解决方案 |
|------|----------|
| 端口已被占用 | 修改配置中的端口或杀死占用进程 |
| Docker 无法启动 | 检查 Docker Desktop 是否运行 |
| 数据库错误 | 验证凭据和连接 |
| 编译失败 | 清理缓存: `cargo clean && npm clean-install` |
| WebSocket 错误 | 检查后端是否运行且可访问 |
**祝您交易愉快！🚀📈**

如有问题，请查看日志和文档。
如需功能请求，请更新策略配置。
**文档版本**: v2.0.0
**最后更新**: 2025-12-21